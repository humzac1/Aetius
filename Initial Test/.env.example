# Copy to .env and fill in. Never commit the real .env.
# Only needed for the "real" arm — the mock backend (provider="mock" in a
# SystemConfig) runs with zero API spend and needs none of this.

ANTHROPIC_API_KEY=
